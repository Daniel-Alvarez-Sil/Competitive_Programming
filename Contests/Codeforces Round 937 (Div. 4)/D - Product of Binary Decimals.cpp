#include <bits/stdc++.h>

using namespace std; 

string resolver(){
    int n = 0; 
    cin >> n; 
    int div = 11; 
    while(n > 1 && div ){
        
    }
}

int main(){
    int t = 0; 
    cin >> t; 
    vector<string> resultados(t); 
    for (int f = 0; f < t; f++)
        resultados[f] = resolver(); 
    for (const auto& x: resultados)
        cout << x << '\n'; 
    return 0; 
}